{
  "name": "imgSearch",
  "homepage": "https://BaranovAleksei.github.io/imgSearch",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html --public-url /imgSearch --no-content-hash",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "gh-pages": "^3.2.2"
  }
}
